# ==============================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ==============================================
# WARNING: NEVER commit this file with real values!
# Use secure secret management in production

# Database - Use strong credentials and SSL
DATABASE_URL="postgresql://nova_hr_user:STRONG_DB_PASSWORD@db.example.com:5432/nova_hr_prod?schema=public&sslmode=require"

# JWT - Use cryptographically strong secrets (256-bit minimum)
# Generate with: openssl rand -base64 32
JWT_SECRET="REPLACE_WITH_STRONG_256_BIT_SECRET_KEY"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="REPLACE_WITH_DIFFERENT_STRONG_256_BIT_SECRET"
JWT_REFRESH_EXPIRES_IN="7d"

# App Configuration
NODE_ENV="production"
PORT=3000
API_PREFIX="api/v1"

# CORS - Restrict to production domains only
CORS_ORIGIN="https://yourdomain.com,https://app.yourdomain.com,https://admin.yourdomain.com"

# File Upload - AWS S3 Production
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID="AKIAIOSFODNN7EXAMPLE"
AWS_SECRET_ACCESS_KEY="wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
S3_BUCKET="nova-hr-prod"
S3_ENDPOINT=""
S3_FORCE_PATH_STYLE=false

# Email - Production SMTP (AWS SES, SendGrid, etc.)
SMTP_HOST="email-smtp.us-east-1.amazonaws.com"
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER="AKIAIOSFODNN7EXAMPLE"
SMTP_PASS="SMTP_PASSWORD"
SMTP_FROM="noreply@yourdomain.com"

# Rate Limiting - More strict in production
THROTTLE_TTL=60
THROTTLE_LIMIT=30

# External APIs
MAPS_API_KEY="YOUR_GOOGLE_MAPS_API_KEY"
RECAPTCHA_SECRET_KEY="6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe"

# Monitoring & Logging
SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"
LOG_LEVEL="warn"

# Security Headers
HELMET_CSP_DIRECTIVES="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' https:; connect-src 'self' https:; media-src 'self'; object-src 'none'; child-src 'none'; worker-src 'none'; frame-ancestors 'none'; form-action 'self'; base-uri 'self'"

# Encryption Keys (for sensitive data at rest)
# Generate with: openssl rand -base64 32
DATA_ENCRYPTION_KEY="REPLACE_WITH_STRONG_ENCRYPTION_KEY"
COOKIE_SECRET="REPLACE_WITH_COOKIE_SECRET"

# Database Connection Pool
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# Redis (for sessions/cache) - if using
REDIS_URL="redis://username:password@redis.example.com:6379"

# SSL/TLS Settings
SSL_KEY_PATH="/path/to/private-key.pem"
SSL_CERT_PATH="/path/to/certificate.pem"

# Company Settings
DEFAULT_TIMEZONE="Asia/Jakarta"
DEFAULT_CURRENCY="IDR"

# Feature Flags
ENABLE_SWAGGER=false
ENABLE_DEBUG=false
ENABLE_METRICS=true

# Backup & Recovery
BACKUP_ENCRYPTION_KEY="REPLACE_WITH_BACKUP_ENCRYPTION_KEY"